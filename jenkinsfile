pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'docker build -t ims_project .'
            }
        }

        stage('Run') {
            steps {
                sh 'docker run -d -p 8000:8000 ims_project'
            }
        }
    }
}